import React from "react";

const DatabasePage = () => {
    return (
        <div className="widget page">
            <h1>Game Database</h1>
            
            <div className="box watch">
                <h3>Watch a Game</h3>
                <button onClick={() => { }} className="button">Load Game File</button>

                <button onClick={() => { }} className="button">Load Game Database</button>
            </div>


            {/* ──────────────────────────────────────────────────────────────────────────── */ }
        </div>
    );
};

export default DatabasePage;